# Batch FX Execution Testing / Demo SPA

1. MyHubFx is a SPA app to test buying FX in batches via Capitec Bank APIs.
2. The main app files are `index.html`, `js/app.js`, `css/style.css` and `api.php`.
3. Project structure is documented in `docs/STRUCTURE.md`.
4. Bank / Trading API docs are in `docs/api/capitec-api`.
5. Project requirements are in `docs/PRD.md` and `docs/PLAN.md`.
6. Tools, utilities, and usage instructions in `tools`, `tools/README`.
7. Test code is in the `tests` folder.
8. Database: `data/tradedesk.db`.
9. Debug log: `debug.log`.
10. Sample data: `data/DEALS.csv`, `data/batch.csv`.